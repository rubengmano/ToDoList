<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>To Do List</title>
  </head>
  <body>

    <%# In order to work is needed the scriplet tag  %>
    <% if(weekDay === 'Saturday' || weekDay === 'Sunday'){ %>
      <!-- Add a marker to place a particular file -->
      <h1 style="color: purple"><%= weekDay %> ToDo List</h1>
    <% } else { %>
      <h1 style="color: blue"><%= weekDay %> ToDo List</h1>
    <% } %>

  </body>
</html>
